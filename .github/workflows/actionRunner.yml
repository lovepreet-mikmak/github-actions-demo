name: push-task-runner
on:
  push:
    branches: 
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: echo "This job is automaticaly triggered on ${{github.event_name}} "
    - run: echo "Thank you"
  start:
    runs-on : ubuntu-latest
    steps:
    - run: echo "we are now hiting npm start"
    - run: npm start